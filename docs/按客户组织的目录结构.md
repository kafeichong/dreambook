🎯 按客户组织的目录结构

  /data/
  ├── hubei-library/              # 湖北图书馆（统一目录）
  │   ├── nginx-shared/          # 共享 Nginx
  │   │   ├── docker-compose.yml
  │   │   ├── conf.d/
  │   │   │   ├── emotion-library.conf
  │   │   │   └── dreambook.conf
  │   │   ├── ssl/
  │   │   └── logs/
  │   │
  │   ├── emotion-library/       # 项目1：情绪图书馆
  │   │   ├── code/
  │   │   │   ├── backend/
  │   │   │   ├── frontend/
  │   │   │   └── docker-compose.yml
  │   │   ├── data/
  │   │   │   ├── postgres/
  │   │   │   └── uploads/
  │   │   └── dist-web/
  │   │
  │   └── dreambook/             # 项目2：梦境解析
  │       ├── code/
  │       │   ├── dreambook-api/
  │       │   ├── dreambook-web/
  │       │   └── docker-compose.yml
  │       ├── data/
  │       │   └── logs/
  │       └── dist-web/
  │
  ├── other-client-1/            # 其他客户的项目
  ├── other-client-2/
  └── ...

  ---
  📝 完整配置

  1. 共享 Nginx

  路径：/data/hubei-library/nginx-shared/docker-compose.yml

  version: '3.8'

  services:
    nginx:
      image: nginx:alpine
      container_name: hubei-library-nginx
      restart: always
      ports:
        - "80:80"
        - "443:443"
      volumes:
        # Nginx 配置
        - ./conf.d:/etc/nginx/conf.d:ro
        - ./ssl:/etc/nginx/ssl:ro
        - ./logs:/var/log/nginx

        # emotion-library 前端
        - /data/hubei-library/emotion-library/dist-web:/usr/share/nginx/html/emotion:ro

        # dreambook 前端
        - /data/hubei-library/dreambook/dist-web:/usr/share/nginx/html/dreambook:ro

      networks:
        - hubei-library-frontend
      healthcheck:
        test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
        interval: 30s
        timeout: 10s
        retries: 3
      deploy:
        resources:
          limits:
            cpus: '0.3'
            memory: 256M
      logging:
        driver: "json-file"
        options:
          max-size: "10m"
          max-file: "3"

  networks:
    hubei-library-frontend:
      name: hubei-library-frontend
      driver: bridge

  ---
  2. emotion-library 项目

  路径：/data/hubei-library/emotion-library/code/docker-compose.yml

  version: '3.8'

  services:
    postgres:
      image: postgres:13-alpine
      container_name: emotion-library-db-prod
      restart: always
      environment:
        POSTGRES_DB: ${DATABASE_NAME}
        POSTGRES_USER: ${DATABASE_USERNAME}
        POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      volumes:
        # 数据存储在项目自己的 data 目录
        - /data/hubei-library/emotion-library/data/postgres:/var/lib/postgresql/data
      networks:
        - emotion-backend
      expose:
        - "5432"
      healthcheck:
        test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USERNAME}"]
        interval: 10s
        timeout: 5s
        retries: 5
      deploy:
        resources:
          limits:
            cpus: '0.8'
            memory: 512M
      logging:
        driver: "json-file"
        options:
          max-size: "10m"
          max-file: "3"

    strapi:
      build:
        context: ./backend
        dockerfile: Dockerfile
        target: production
      image: emotion-library-api-prod:latest
      container_name: emotion-library-api-prod
      restart: always
      environment:
        DATABASE_HOST: postgres
        DATABASE_NAME: ${DATABASE_NAME}
        DATABASE_USERNAME: ${DATABASE_USERNAME}
        DATABASE_PASSWORD: ${DATABASE_PASSWORD}
        HOST: 0.0.0.0
        PORT: 1337
        APP_KEYS: ${APP_KEYS}
        API_TOKEN_SALT: ${API_TOKEN_SALT}
        ADMIN_JWT_SECRET: ${ADMIN_JWT_SECRET}
        TRANSFER_TOKEN_SALT: ${TRANSFER_TOKEN_SALT}
        JWT_SECRET: ${JWT_SECRET}
        NODE_ENV: production
        ADMIN_PATH: ${ADMIN_PATH:-/admin}
      volumes:
        # 上传文件存储在项目自己的 data 目录
        - /data/hubei-library/emotion-library/data/uploads:/app/public/uploads
      networks:
        - emotion-backend
        - hubei-library-frontend  # 连接到共享前端网络
      expose:
        - "1337"
      depends_on:
        postgres:
          condition: service_healthy
      healthcheck:
        test: ["CMD", "node", "-e", "require('http').get('http://localhost:1337/_health', (r) =>
  {process.exit(r.statusCode === 200 ? 0 : 1)})"]
        interval: 30s
        timeout: 10s
        retries: 3
        start_period: 40s
      deploy:
        resources:
          limits:
            cpus: '0.6'
            memory: 2G
      logging:
        driver: "json-file"
        options:
          max-size: "10m"
          max-file: "3"

  networks:
    emotion-backend:
      name: emotion-library-backend
      driver: bridge
      internal: true

    hubei-library-frontend:
      external: true
      name: hubei-library-frontend

  ---
  3. dreambook 项目

  路径：/data/hubei-library/dreambook/code/docker-compose.yml

  version: '3.8'

  services:
    backend:
      build:
        context: ./dreambook-api
        dockerfile: Dockerfile
      image: dreambook-backend:latest
      container_name: dreambook-backend-prod
      restart: always
      environment:
        NODE_ENV: production
        PORT: 3000
        DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
        CORS_ORIGIN: ${CORS_ORIGIN}
        TZ: Asia/Shanghai
      volumes:
        # 日志存储在项目自己的 data 目录
        - /data/hubei-library/dreambook/data/logs:/app/logs
      networks:
        - hubei-library-frontend  # 连接到共享前端网络
      expose:
        - "3000"
      healthcheck:
        test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) =>
  {process.exit(r.statusCode === 200 ? 0 : 1)})"]
        interval: 30s
        timeout: 10s
        retries: 3
      deploy:
        resources:
          limits:
            cpus: '0.2'
            memory: 256M
      logging:
        driver: "json-file"
        options:
          max-size: "10m"
          max-file: "3"

  networks:
    hubei-library-frontend:
      external: true
      name: hubei-library-frontend

  ---
  4. Nginx 配置文件

  emotion-library (/data/hubei-library/nginx-shared/conf.d/emotion-library.conf):

  server {
      listen 80;
      server_name emotion.yourdomain.com;

      # 前端静态文件
      location / {
          root /usr/share/nginx/html/emotion;
          index index.html;
          try_files $uri $uri/ /index.html;
          expires 1h;
      }

      # 静态资源缓存
      location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|webp)$ {
          root /usr/share/nginx/html/emotion;
          expires 7d;
          add_header Cache-Control "public, max-age=604800, immutable";
      }

      # Strapi API 代理
      location /api/ {
          proxy_pass http://emotion-library-api-prod:1337/api/;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;

          proxy_connect_timeout 30s;
          proxy_send_timeout 60s;
          proxy_read_timeout 60s;
      }

      # Strapi Admin
      location /admin {
          proxy_pass http://emotion-library-api-prod:1337/admin;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;
      }

      # Strapi 上传文件
      location /uploads/ {
          proxy_pass http://emotion-library-api-prod:1337/uploads/;
          proxy_set_header Host $host;
      }

      # Gzip 压缩
      gzip on;
      gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

      # 日志
      access_log /var/log/nginx/emotion-access.log;
      error_log /var/log/nginx/emotion-error.log;
  }

  dreambook (/data/hubei-library/nginx-shared/conf.d/dreambook.conf):

  server {
      listen 80;
      server_name dream.yourdomain.com;

      # 前端静态文件
      location / {
          root /usr/share/nginx/html/dreambook;
          index index.html;
          try_files $uri $uri/ /index.html;
          expires 1h;
      }

      # 静态资源缓存
      location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|webp)$ {
          root /usr/share/nginx/html/dreambook;
          expires 7d;
          add_header Cache-Control "public, max-age=604800, immutable";
      }

      # DreamBook API 代理
      location /api/ {
          proxy_pass http://dreambook-backend-prod:3000/api/;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;

          proxy_connect_timeout 10s;
          proxy_send_timeout 30s;
          proxy_read_timeout 30s;
      }

      # 健康检查
      location /health {
          proxy_pass http://dreambook-backend-prod:3000/health;
          access_log off;
      }

      # Gzip 压缩
      gzip on;
      gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

      # 日志
      access_log /var/log/nginx/dream-access.log;
      error_log /var/log/nginx/dream-error.log;
  }

  ---
  🚀 部署步骤

  第一步：创建目录结构

  # SSH 登录服务器
  ssh 131****2933@your-server

  # 创建湖北图书馆项目的根目录
  mkdir -p /data/hubei-library

  # 创建各子目录
  mkdir -p /data/hubei-library/nginx-shared/{conf.d,ssl,logs}
  mkdir -p /data/hubei-library/emotion-library/{code,data,dist-web}
  mkdir -p /data/hubei-library/dreambook/{code,data,dist-web}

  第二步：迁移 emotion-library（如果已存在）

  # 停止旧容器
  cd /data/emotion-library/code
  docker-compose down

  # 迁移代码
  mv /data/emotion-library/code /data/hubei-library/emotion-library/

  # 迁移数据
  mv /data/emotion-library/data/* /data/hubei-library/emotion-library/data/
  mv /data/emotion-library/dist-web /data/hubei-library/emotion-library/

  # 迁移 Nginx 配置
  mv /data/emotion-library/code/docker/nginx/conf.d/* /data/hubei-library/nginx-shared/conf.d/
  mv /data/emotion-library/code/docker/nginx/ssl/* /data/hubei-library/nginx-shared/ssl/

  第三步：部署 dreambook

  # 上传代码（从本地）
  cd /Users/steven/works/20251130dreambook/code
  tar -czf dreambook-deploy.tar.gz dreambook-api dreambook-web deploy \
    --exclude=node_modules --exclude=dist

  scp dreambook-deploy.tar.gz user@server:/tmp/

  # 服务器解压
  ssh user@server
  cd /data/hubei-library/dreambook/code
  tar -xzf /tmp/dreambook-deploy.tar.gz

  # 构建前端
  cd dreambook-web
  npm install
  npm run build
  cp -r dist/* /data/hubei-library/dreambook/dist-web/

  第四步：创建配置文件

  cd /data/hubei-library

  # 创建共享 Nginx 的 docker-compose.yml
  vim nginx-shared/docker-compose.yml
  # 粘贴上面的配置

  # 创建 Nginx 配置文件
  vim nginx-shared/conf.d/emotion-library.conf
  vim nginx-shared/conf.d/dreambook.conf

  # 更新 emotion-library 的 docker-compose.yml
  vim emotion-library/code/docker-compose.yml
  # 更新网络名为 hubei-library-frontend

  # 创建 dreambook 的 docker-compose.yml
  vim dreambook/code/docker-compose.yml
  # 粘贴上面的配置

  第五步：启动服务

  # 1. 启动共享 Nginx（创建网络）
  cd /data/hubei-library/nginx-shared
  docker-compose up -d

  # 验证网络创建
  docker network ls | grep hubei-library

  # 2. 启动 emotion-library
  cd /data/hubei-library/emotion-library/code
  docker-compose up -d

  # 3. 启动 dreambook
  cd /data/hubei-library/dreambook/code
  docker-compose up -d

  # 4. 查看所有容器
  docker ps

  第六步：验证

  # 检查网络连接
  docker network inspect hubei-library-frontend

  # 应该看到：
  # - hubei-library-nginx
  # - emotion-library-api-prod
  # - dreambook-backend-prod

  # 测试访问
  curl http://localhost/
  curl http://localhost/health

  ---
  📊 优化对比
  ┌────────────┬──────────────────────────────────────────┬──────────────────────┐
  │    方面    │                  旧结构                  │        新结构        │
  ├────────────┼──────────────────────────────────────────┼──────────────────────┤
  │ 组织方式   │ 按项目分散                               │ 按客户集中           │
  ├────────────┼──────────────────────────────────────────┼──────────────────────┤
  │ 根目录     │ /data/emotion-library/, /data/dreambook/ │ /data/hubei-library/ │
  ├────────────┼──────────────────────────────────────────┼──────────────────────┤
  │ 管理便利性 │ ⚠️ 分散在不同位置                        │ ✅ 统一在一个目录    │
  ├────────────┼──────────────────────────────────────────┼──────────────────────┤
  │ 备份       │ 需要备份多个目录                         │ 一个目录全备份       │
  ├────────────┼──────────────────────────────────────────┼──────────────────────┤
  │ 权限管理   │ 分散设置                                 │ 统一设置             │
  ├────────────┼──────────────────────────────────────────┼──────────────────────┤
  │ 查找项目   │ 需要记住多个路径                         │ 进入客户目录即可     │
  └────────────┴──────────────────────────────────────────┴──────────────────────┘
  ---
  💡 日常管理

  管理湖北图书馆的所有项目

  # 进入客户根目录
  cd /data/hubei-library/

  # 查看所有项目
  ls -la
  # emotion-library/
  # dreambook/
  # nginx-shared/

  # 统一备份整个客户的项目
  tar -czf hubei-library-backup-$(date +%Y%m%d).tar.gz hubei-library/

  # 查看所有相关容器
  docker ps | grep -E "emotion|dreambook|hubei"

  # 查看共享网络
  docker network inspect hubei-library-frontend

  添加新项目（湖北图书馆的第三个项目）

  # 1. 创建项目目录
  mkdir -p /data/hubei-library/new-project/{code,data,dist-web}

  # 2. 上传代码
  # ...

  # 3. 创建 docker-compose.yml（连接到 hubei-library-frontend 网络）

  # 4. 在 Nginx 添加配置
  vim /data/hubei-library/nginx-shared/conf.d/new-project.conf

  # 5. 重载 Nginx
  docker exec hubei-library-nginx nginx -s reload

  # 6. 启动新项目
  cd /data/hubei-library/new-project/code
  docker-compose up -d

  ---
  ✅ 最终目录结构总结

  /data/hubei-library/              # 湖北图书馆统一根目录 ⭐
  ├── nginx-shared/                 # 共享 Nginx（所有项目的前端入口）
  ├── emotion-library/              # 项目1：情绪图书馆
  │   ├── code/                    # 代码
  │   ├── data/                    # 数据
  │   └── dist-web/                # 前端构建产物
  └── dreambook/                    # 项目2：梦境解析
      ├── code/                    # 代码
      ├── data/                    # 数据
      └── dist-web/                # 前端构建产物

  优势：
  1. ✅ 统一客户目录，便于管理
  2. ✅ 项目独立，互不干扰
  3. ✅ 共享 Nginx，节省资源
  4. ✅ 清晰的层级结构
  5. ✅ 便于备份和迁移

  需要我帮你生成完整的部署脚本吗？