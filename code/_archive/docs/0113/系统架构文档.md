# DreamBook Kiosk 系统架构文档

> 系统架构、组件交互和数据流可视化文档

---

## 📊 系统架构概览

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        Windows 10 操作系统                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                       Kiosk 用户会话                        │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │           DreamBook Electron 应用                    │  │  │
│  │  │                                                       │  │  │
│  │  │  ┌──────────────┐         ┌──────────────┐          │  │  │
│  │  │  │  主进程       │◄────────┤  渲染进程     │          │  │  │
│  │  │  │  (main.ts)   │  IPC    │  (React UI)  │          │  │  │
│  │  │  └──────┬───────┘         └──────┬───────┘          │  │  │
│  │  │         │                         │                   │  │  │
│  │  │         │ ┌───────────────────────┘                  │  │  │
│  │  │         │ │                                           │  │  │
│  │  │  ┌──────▼─▼────────┐                                 │  │  │
│  │  │  │   preload.ts    │                                 │  │  │
│  │  │  │  (IPC Bridge)   │                                 │  │  │
│  │  │  └─────────────────┘                                 │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │                                                             │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │             后端服务 (Express.js)                    │  │  │
│  │  │                                                       │  │  │
│  │  │  ┌──────────┐    ┌──────────┐    ┌──────────┐      │  │  │
│  │  │  │ REST API │───►│ DeepSeek │───►│  AI 模型  │      │  │  │
│  │  │  │ :3000    │    │  Service │    │  (云端)   │      │  │  │
│  │  │  └──────────┘    └──────────┘    └──────────┘      │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │               Windows 系统服务                             │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │  │
│  │  │ TabTip.exe   │  │ tsdiscon.exe │  │  注册表服务   │   │  │
│  │  │ (虚拟键盘)   │  │ (切换用户)   │  │  (自动登录)   │   │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 组件交互流程

### 1. 系统启动流程

```
开机
  │
  ├──► Windows 加载
  │      │
  │      ├──► 读取注册表: AutoAdminLogon=1
  │      │      │
  │      │      └──► 自动登录到 kiosk 用户 (无密码输入)
  │      │
  │      └──► 读取 Shell 配置: dreambook.exe
  │             │
  │             └──► 启动 dreambook.exe (替代 explorer.exe)
  │
  ├──► Electron 主进程启动 (main.ts)
  │      │
  │      ├──► 创建 BrowserWindow
  │      ├──► 启用 GPU 加速
  │      ├──► 设置全屏无边框 (生产环境)
  │      ├──► 加载 preload.ts
  │      └──► 启动后端进程
  │
  ├──► 后端服务启动 (Express.js)
  │      │
  │      ├──► 监听端口 3000
  │      ├──► 初始化 DeepSeek API
  │      └──► 准备接受请求
  │
  └──► 渲染进程启动 (React)
         │
         ├──► 加载 React 应用
         ├──► 初始化路由 (HashRouter)
         ├──► 加载动画背景
         └──► 显示首页 ✅
```

---

### 2. 虚拟键盘弹出流程

```
用户点击输入框
  │
  ├──► 触发 onFocus / onClick / onTouchEnd 事件
  │      │
  │      └──► 调用 useVirtualKeyboard Hook
  │             │
  │             ├──► 检查防抖 (500ms)
  │             │      │
  │             │      ├──► 未过期 → 忽略
  │             │      └──► 已过期 → 继续
  │             │
  │             └──► 调用 window.electronAPI.showVirtualKeyboard()
  │
  ├──► IPC 消息: 'show-virtual-keyboard'
  │      │
  │      └──► preload.ts 转发消息
  │             │
  │             └──► 发送到主进程
  │
  ├──► 主进程接收 IPC (main.ts)
  │      │
  │      ├──► 检查平台: process.platform === 'win32'
  │      │      │
  │      │      ├──► 非 Windows → 跳过
  │      │      └──► Windows → 继续
  │      │
  │      └──► 调用 showWindowsVirtualKeyboard()
  │             │
  │             ├──► 方法 1: PowerShell COM 接口 (Win11)
  │             │      │
  │             │      ├──► 成功 → 返回 ✅
  │             │      └──► 失败 → 尝试方法 2
  │             │
  │             ├──► 方法 2: TabTip.exe (Win10)
  │             │      │
  │             │      ├──► 启动 TabTip.exe
  │             │      ├──► 成功 → 返回 ✅
  │             │      └──► 失败 → 尝试方法 3
  │             │
  │             ├──► 方法 3: URI 协议
  │             │      │
  │             │      ├──► 打开 ms-virtualkeyboard://
  │             │      ├──► 成功 → 返回 ✅
  │             │      └──► 失败 → 尝试方法 4
  │             │
  │             └──► 方法 4: osk.exe (备用)
  │                    │
  │                    ├──► 启动 osk.exe
  │                    └──► 返回结果
  │
  └──► 虚拟键盘弹出 ✅
         │
         └──► 用户可以输入文字
```

---

### 3. 管理面板打开流程

```
用户在右上角点击
  │
  ├──► AdminTrigger 组件检测点击
  │      │
  │      ├──► 记录点击时间
  │      ├──► 增加计数器
  │      │
  │      └──► 检查条件:
  │             ├──► 点击次数 >= 5?
  │             └──► 时间间隔 < 2 秒?
  │                    │
  │                    ├──► 否 → 重置计数器
  │                    └──► 是 → 打开管理面板
  │
  ├──► AdminPanel 组件显示
  │      │
  │      └──► 显示 4 个按钮:
  │             ├──► 刷新页面
  │             ├──► 重启应用
  │             ├──► 退出应用
  │             └──► 切换用户 ✅
  │
  └──► 用户选择操作
```

---

### 4. 切换用户流程

```
用户点击"切换用户"按钮
  │
  ├──► AdminPanel.handleSwitchUser()
  │      │
  │      ├──► 检查 API 可用性
  │      │      │
  │      │      ├──► 不可用 → 显示错误提示
  │      │      └──► 可用 → 继续
  │      │
  │      └──► 显示确认对话框
  │             │
  │             ├──► 用户取消 → 终止
  │             └──► 用户确认 → 继续
  │
  ├──► 调用 window.electronAPI.switchUser()
  │      │
  │      └──► IPC 消息: 'sys:switch-user'
  │
  ├──► 主进程接收 IPC (main.ts)
  │      │
  │      ├──► 检查平台: process.platform === 'win32'
  │      │      │
  │      │      ├──► 非 Windows → 跳过
  │      │      └──► Windows → 继续
  │      │
  │      ├──► 执行 tsdiscon.exe
  │      │      │
  │      │      └──► 断开当前用户会话
  │      │
  │      └──► 设置 isQuitting = true
  │             │
  │             └──► 防止应用自动重启
  │
  └──► Windows 登录界面显示 ✅
         │
         └──► 可以登录其他账户 (如 Administrator)
```

---

### 5. AI 解梦流程

```
用户输入梦境描述
  │
  ├──► 点击"开始解析"
  │      │
  │      └──► 发送 POST 请求到 localhost:3000/api/dream-chat
  │             │
  │             └──► 请求体: { question: "梦境描述" }
  │
  ├──► 后端接收请求 (Express.js)
  │      │
  │      ├──► 解析请求体
  │      ├──► 验证输入
  │      │
  │      └──► 调用 DeepSeek 服务
  │             │
  │             ├──► 构造系统提示词
  │             ├──► 添加用户消息
  │             │
  │             └──► 发送请求到 DeepSeek API
  │                    │
  │                    └──► https://api.deepseek.com/v1/chat/completions
  │
  ├──► AI 模型处理 (云端)
  │      │
  │      ├──► 分析梦境内容
  │      ├──► 生成解析结果
  │      │
  │      └──► 返回响应
  │
  ├──► 后端接收 AI 响应
  │      │
  │      ├──► 提取答案内容
  │      ├──► 格式化响应
  │      │
  │      └──► 返回给前端: { answer: "解析结果" }
  │
  └──► 前端显示结果 ✅
         │
         ├──► 显示 AI 解析内容
         └──► 用户可以查看详情
```

---

## 📦 数据流图

### IPC 通信数据流

```
┌─────────────┐                    ┌─────────────┐
│  渲染进程    │                    │   主进程     │
│  (React UI) │                    │  (Node.js)  │
└──────┬──────┘                    └──────┬──────┘
       │                                  │
       │  1. 用户操作触发                  │
       │  (点击输入框、点击按钮)            │
       │                                  │
       │  2. 调用 window.electronAPI.xxx() │
       │─────────────────────────────────►│
       │                                  │
       │  3. preload.ts 转发 IPC          │
       │     ipcRenderer.invoke()         │
       │─────────────────────────────────►│
       │                                  │
       │                                  │  4. 主进程处理
       │                                  │     ipcMain.handle()
       │                                  │
       │                                  │  5. 执行系统命令
       │                                  │     (TabTip.exe, tsdiscon.exe)
       │                                  │
       │  6. 返回结果                      │
       │◄─────────────────────────────────│
       │     Promise.resolve()            │
       │                                  │
       │  7. 更新 UI                       │
       │  (虚拟键盘弹出、面板显示)         │
       │                                  │
```

### HTTP 请求数据流

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│  React UI   │         │  Express.js │         │ DeepSeek API│
└──────┬──────┘         └──────┬──────┘         └──────┬──────┘
       │                       │                       │
       │ 1. 用户输入梦境         │                       │
       │                       │                       │
       │ 2. POST /api/dream-chat│                       │
       │──────────────────────►│                       │
       │   { question: "..." } │                       │
       │                       │                       │
       │                       │ 3. 构造 AI 请求        │
       │                       │                       │
       │                       │ 4. POST /v1/chat/completions
       │                       │──────────────────────►│
       │                       │   {                   │
       │                       │     model: "...",     │
       │                       │     messages: [...],  │
       │                       │   }                   │
       │                       │                       │
       │                       │                       │ 5. AI 处理
       │                       │                       │
       │                       │ 6. 返回 AI 响应        │
       │                       │◄──────────────────────│
       │                       │   {                   │
       │                       │     choices: [...],   │
       │                       │   }                   │
       │                       │                       │
       │                       │ 7. 提取答案内容        │
       │                       │                       │
       │ 8. 返回结果             │                       │
       │◄──────────────────────│                       │
       │   { answer: "..." }   │                       │
       │                       │                       │
       │ 9. 显示解析结果         │                       │
       │                       │                       │
```

---

## 🔐 安全架构

### 权限隔离模型

```
┌─────────────────────────────────────────────────────────┐
│                     Windows 10 系统                      │
│                                                          │
│  ┌────────────────────┐      ┌────────────────────┐   │
│  │  kiosk 用户         │      │ Administrator 用户 │   │
│  │  (受限权限)         │      │  (完整权限)        │   │
│  │                     │      │                    │   │
│  │  ✅ 运行 dreambook   │      │  ✅ 系统配置        │   │
│  │  ✅ 访问应用目录     │      │  ✅ 安装软件        │   │
│  │  ✅ 使用虚拟键盘     │      │  ✅ 修改注册表      │   │
│  │                     │      │  ✅ 用户管理        │   │
│  │  ❌ 系统配置         │      │                    │   │
│  │  ❌ 安装软件         │      │                    │   │
│  │  ❌ 修改注册表       │      │                    │   │
│  │  ❌ 访问其他用户文件 │      │                    │   │
│  └────────────────────┘      └────────────────────┘   │
│                                                          │
│  ┌───────────────────────────────────────────────────┐ │
│  │              进程隔离                              │ │
│  │                                                    │ │
│  │  ┌──────────────┐  ┌──────────────┐             │ │
│  │  │ Electron App │  │  Backend API │             │ │
│  │  │  (主进程)     │  │ (Express.js) │             │ │
│  │  │              │  │              │             │ │
│  │  │ ✅ 系统命令    │  │ ✅ HTTP API   │             │ │
│  │  │ ✅ IPC 通信    │  │ ✅ DeepSeek   │             │ │
│  │  │ ❌ 直接访问网络│  │ ❌ 文件系统    │             │ │
│  │  └──────────────┘  └──────────────┘             │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### API Key 安全模型

```
┌─────────────────────────────────────────────────────────┐
│                     安全边界                             │
│                                                          │
│  ┌────────────────────┐                                │
│  │   前端 (React)      │                                │
│  │                     │                                │
│  │  ❌ 无 API Key       │                                │
│  │  ❌ 无敏感信息       │                                │
│  │  ✅ 只发送用户输入   │                                │
│  └────────┬───────────┘                                │
│           │ HTTP 请求                                   │
│           │ (localhost only)                            │
│           ▼                                              │
│  ┌────────────────────┐                                │
│  │   后端 (Express)    │                                │
│  │                     │                                │
│  │  ✅ API Key 存储     │  ← backend/.env (gitignore)   │
│  │  ✅ 请求验证         │                                │
│  │  ✅ 错误处理         │                                │
│  └────────┬───────────┘                                │
│           │ HTTPS 请求                                  │
│           │ (加密传输)                                   │
│           ▼                                              │
│  ┌────────────────────┐                                │
│  │  DeepSeek API      │                                │
│  │  (云端服务)         │                                │
│  └────────────────────┘                                │
└─────────────────────────────────────────────────────────┘
```

---

## 🗂️ 文件系统结构

```
C:\
├── Windows\
│   ├── System32\
│   │   ├── tsdiscon.exe          # 切换用户命令
│   │   └── ...
│   └── ...
│
├── Program Files\
│   └── Common Files\
│       └── microsoft shared\
│           └── ink\
│               └── TabTip.exe    # 虚拟键盘程序
│
├── Users\
│   ├── Default\
│   │   └── NTUSER.DAT           # Shell 配置存储
│   │
│   ├── kiosk\                    # Kiosk 用户目录
│   │   ├── Desktop\              # (空，无桌面)
│   │   ├── Documents\
│   │   └── dreambook-logs\       # 应用日志
│   │       └── app-2026-01-13.log
│   │
│   └── Administrator\            # 管理员用户目录
│       └── ...
│
└── kiosk\                        # 应用安装目录
    └── dreambook\
        ├── dreambook.exe         # 主程序
        ├── resources\
        │   └── app.asar          # 应用资源
        └── locales\
            └── ...
```

---

## 🔄 状态机图

### 应用生命周期状态

```
     [开机]
        │
        ▼
   [系统启动]
        │
        ├──► 自动登录
        │
        ▼
  [应用启动中]
        │
        ├──► 主进程初始化
        ├──► 后端服务启动
        └──► 渲染进程加载
        │
        ▼
   [应用运行中] ◄────────┐
        │                 │
        ├──► [空闲状态]    │
        │                 │
        ├──► [输入状态]    │ 用户操作
        │      └──► 虚拟键盘 │
        │                 │
        ├──► [AI处理中]    │
        │      └──► 调用后端 │
        │                 │
        └──► [管理模式]    │
               │          │
               ├──► 刷新──┘
               │
               ├──► 重启
               │      │
               │      └──► [应用重启中]
               │              │
               │              └──► [应用运行中]
               │
               ├──► 退出
               │      │
               │      └──► [应用退出]
               │              │
               │              └──► [系统桌面]
               │
               └──► 切换用户
                      │
                      └──► [登录界面]
                              │
                              ├──► Administrator ──► [系统桌面]
                              │
                              └──► kiosk ──────────► [应用运行中]
```

---

## 📊 性能监控点

### 关键性能指标 (KPI)

```
应用启动
   │
   ├──► [0s] 系统启动
   │
   ├──► [15s] 自动登录完成
   │
   ├──► [20s] 主进程启动
   │      └──► 监控: CPU < 30%, 内存 < 150MB
   │
   ├──► [25s] 后端服务启动
   │      └──► 监控: 端口 3000 监听
   │
   ├──► [30s] 渲染进程加载
   │      └──► 监控: 首屏渲染时间 < 2s
   │
   └──► [30s] 应用就绪 ✅
          │
          └──► 监控: 总启动时间 < 30s

运行时
   │
   ├──► 空闲状态
   │      └──► 监控: CPU < 5%, 内存 < 300MB
   │
   ├──► 虚拟键盘
   │      └──► 监控: 弹出延迟 < 1s
   │
   ├──► AI 处理
   │      └──► 监控: 响应时间 < 10s
   │
   └──► 管理面板
          └──► 监控: 打开延迟 < 500ms
```

---

## 🔌 外部依赖

```
┌─────────────────────────────────────────────────────────┐
│                   DreamBook 应用                         │
└───────────────────┬─────────────────────────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
┌──────────┐ ┌──────────┐ ┌──────────────┐
│ Windows  │ │ DeepSeek │ │   Node.js    │
│ System   │ │   API    │ │  Runtime     │
│          │ │          │ │              │
│ • TabTip │ │ • Chat   │ │ • Electron   │
│ • tsdiscon│ │ • Model  │ │ • Express    │
│ • Registry│ │ • Auth   │ │ • React      │
└──────────┘ └──────────┘ └──────────────┘
```

---

**最后更新:** 2026-01-13
**版本:** 1.0
**状态:** ✅ 完成
