# 情绪星球 - 梦境解析触摸屏应用 · 完整开发计划

> **项目代号：** Emotion Planet - Dream Analysis Interactive
> **运行环境：** 图书馆触摸屏设备（Windows 10，1920×1080，Electron）
> **开发周期：** 预计 3-4 周
> **文档版本：** v2.0（综合版）
> **创建日期：** 2025-11-30

---

## 📑 目录

1. [项目总览](#1-项目总览)
2. [技术架构](#2-技术架构)
3. [资源准备阶段](#3-资源准备阶段)
4. [开发阶段规划](#4-开发阶段规划)
5. [测试验收标准](#5-测试验收标准)
6. [部署上线流程](#6-部署上线流程)
7. [时间规划表](#7-时间规划表)
8. [风险控制](#8-风险控制)
9. [附录：技术决策记录](#9-附录技术决策记录)

---

## 1. 项目总览

### 1.1 项目背景

**使用场景：**
- 图书馆公共触摸屏设备
- 全天候运行（8:00-22:00，约14小时）
- 无人值守，需要高稳定性

**目标用户：**
- 图书馆访客（学生、读者）
- 年龄范围：18-35岁为主
- 使用时长：单次 3-10 分钟

**核心价值：**
- 提供梦境解析服务
- 心理健康科普
- 情绪疏导建议

### 1.2 功能概述

```
核心功能流程：
首页（情绪星球介绍）
  ↓ [点击开始探索]
导航页（10个梦境场景选择 + 搜索）
  ↓ [选择梦境类型]
详情页（梦境解析 + 应对建议）
  ↓ [返回主页或继续探索]
```

**页面清单：**
- 首页（Home）
- 导航页（Navigation）
- 详情页（Detail）×10
- 设置页（Settings，可选）

### 1.3 设计风格关键词

```
霓散光 · 梦境 · 柔和 · 缓慢 · 安静 · 有情绪
```

**核心视觉元素：**
- 紫蓝渐变背景（#8B5CF6 → #3B82F6）
- 星空粒子系统
- 毛玻璃（Glassmorphism）UI
- 柔和发光效果
- 流动的云雾纹理

---

## 2. 技术架构

### 2.1 技术栈确认

| 技术 | 版本 | 用途 | 必选/可选 |
|------|------|------|----------|
| **React** | 18.2+ | UI 框架 | ✅ 必选 |
| **Vite** | 5.0+ | 构建工具 | ✅ 必选 |
| **TypeScript** | 5.0+ | 类型安全 | ✅ 必选 |
| **GSAP** | 3.12+ | UI 动画引擎 | ✅ 必选 |
| **React Router** | 6.20+ | 页面路由 | ✅ 必选 |
| **Canvas 2D API** | - | 基础粒子背景 | ✅ 必选 |
| **Three.js** | r160+ | 高级3D背景 | ⭐ 可选增强 |
| **Electron** | 28+ | 桌面打包 | ✅ 必选 |
| **CSS Modules** | - | 样式隔离 | ✅ 必选 |

### 2.2 分层架构设计

```
┌─────────────────────────────────────────────────┐
│ Layer 3: React DOM + GSAP (前景层)               │
│ - 所有 UI 组件（卡片、按钮、文字）                │
│ - 页面路由与交互逻辑                             │
│ - GSAP 驱动的进场/转场动画                       │
├─────────────────────────────────────────────────┤
│ Layer 2: CSS + Canvas 2D (中景层)                │
│ - 毛玻璃效果（backdrop-filter）                  │
│ - 渐变背景（CSS gradient）                       │
│ - Canvas 2D 粒子系统（200-500个）                │
├─────────────────────────────────────────────────┤
│ Layer 1: Three.js WebGL (背景层) - 可选增强      │
│ - 云雾流动效果（Shader）                         │
│ - 高级粒子系统（3000-5000个）                    │
│ - 交互式光效                                     │
└─────────────────────────────────────────────────┘
```

### 2.3 项目目录结构

```
dreambook/
├── public/                      # 静态资源
│   ├── assets/                  # 背景图、插图
│   │   ├── backgrounds/
│   │   │   ├── home-bg-2k.webp
│   │   │   ├── nav-bg-2k.webp
│   │   │   └── detail-bg-2k.webp
│   │   └── dreams/              # 10个梦境配图
│   │       ├── dream-01.webp
│   │       └── ...
│   └── data/
│       └── dreamData.json       # 梦境数据
├── src/
│   ├── components/              # 组件库
│   │   ├── Background/
│   │   │   ├── GradientBackground.tsx
│   │   │   ├── ParticleCanvas.tsx
│   │   │   └── ThreeBackground.tsx (可选)
│   │   ├── Card/
│   │   │   ├── DreamCard.tsx
│   │   │   ├── DreamCard.module.css
│   │   │   └── GlassCard.tsx
│   │   ├── Navigation/
│   │   │   └── HomeButton.tsx
│   │   └── Settings/
│   │       └── PerformanceToggle.tsx
│   ├── pages/
│   │   ├── HomePage/
│   │   │   ├── HomePage.tsx
│   │   │   └── HomePage.module.css
│   │   ├── NavigationPage/
│   │   │   ├── NavigationPage.tsx
│   │   │   └── NavigationPage.module.css
│   │   └── DetailPage/
│   │       ├── DetailPage.tsx
│   │       └── DetailPage.module.css
│   ├── animations/              # GSAP 动画配置
│   │   ├── pageTransitions.ts
│   │   ├── cardAnimations.ts
│   │   └── emotionMappings.ts
│   ├── hooks/                   # React Hooks
│   │   ├── usePerformanceMonitor.ts
│   │   ├── useIdleDetection.ts
│   │   └── usePageTransition.ts
│   ├── utils/
│   │   ├── performanceMonitor.ts
│   │   └── emotionConfig.ts
│   ├── types/
│   │   └── dream.d.ts
│   ├── styles/
│   │   ├── variables.css        # CSS 变量
│   │   └── global.css           # 全局样式
│   ├── App.tsx
│   ├── main.tsx
│   └── vite-env.d.ts
├── electron/
│   ├── main.js                  # Electron 主进程
│   ├── preload.js               # 预加载脚本
│   └── package.json
├── scripts/
│   └── convert-images.sh        # 图片批量转换脚本
├── docs/                        # 项目文档
│   ├── 项目执行规范.md
│   ├── 动画规则1.md
│   ├── 10个梦境数据.md
│   └── 完整开发计划.md (本文档)
├── package.json
├── vite.config.ts
├── tsconfig.json
└── README.md
```

### 2.4 性能优化策略

#### 双模式架构（核心策略）

```javascript
// 性能模式配置
const PERFORMANCE_MODES = {
  HIGH: {
    name: '高品质模式',
    useThreeJS: true,
    particleCount: 5000,
    shaderQuality: 'high',
    targetDevices: ['独显', '高端集显']
  },
  STANDARD: {
    name: '标准模式',
    useThreeJS: false,
    particleCount: 300,
    canvas2D: true,
    targetDevices: ['标准集显', '低端独显']
  },
  ECO: {
    name: '省电模式',
    useThreeJS: false,
    particleCount: 0,
    cssOnly: true,
    targetDevices: ['低端集显', '性能不足设备']
  }
}
```

#### 性能监控指标

```javascript
const PERFORMANCE_METRICS = {
  targetFPS: 60,
  minFPS: 50,              // 低于此值触发降级
  maxMemory: 500,          // MB，超过触发GC
  checkInterval: 5000,     // 每5秒检查一次
  idleTimeout: 180000      // 3分钟无操作进入省电模式
}
```

---

## 3. 资源准备阶段

### 3.1 背景图资源准备

#### 📋 背景图清单

| 页面 | 文件名 | 尺寸 | 格式 | 质量 | 预期大小 |
|-----|--------|------|------|------|---------|
| 首页 | home-bg-2k.webp | 2560×1440 | WebP | 90 | 250-350KB |
| 导航页 | nav-bg-2k.webp | 2560×1440 | WebP | 90 | 250-350KB |
| 详情页 | detail-bg-2k.webp | 2560×1440 | WebP | 90 | 250-350KB |

#### 🎨 导出流程

**Step 1: Figma 导出 PNG**

```
操作：
1. 打开 Figma 设计稿
2. 选中画板（Frame）
3. 右侧面板 → Export
4. 添加导出配置：
   - 格式：PNG
   - 倍率：1.33x
5. 导出文件名：
   - home-bg.png
   - nav-bg.png
   - detail-bg.png
```

**Step 2: 批量转换 WebP**

创建转换脚本 `scripts/convert-images.sh`：

```bash
#!/bin/bash

# 配置
QUALITY=90
WIDTH=2560
HEIGHT=1440

echo "🚀 开始批量转换背景图..."

# 创建输出目录
mkdir -p public/assets/backgrounds

# 转换背景图
for file in design-exports/backgrounds/*.png; do
  [ -f "$file" ] || continue
  filename=$(basename "$file" .png)

  cwebp -resize $WIDTH $HEIGHT -q $QUALITY \
    "$file" \
    -o "public/assets/backgrounds/${filename}-2k.webp"

  echo "✅ $filename.png → ${filename}-2k.webp"
done

echo "🎉 背景图转换完成！"
```

**Step 3: 验证输出**

```bash
# 检查文件大小
ls -lh public/assets/backgrounds/

# 检查尺寸
for f in public/assets/backgrounds/*.webp; do
  echo "$f: $(identify "$f" | grep -o '[0-9]*x[0-9]*')"
done
```

### 3.2 梦境配图准备

#### 📋 配图清单（10个）

| ID | 梦境名称 | 源文件 | 输出文件 | 尺寸 |
|----|---------|--------|---------|------|
| 01 | 坠落 | 坠落.jpeg | dream-01.webp | 1200×800 |
| 02 | 受伤/生病 | 生病.jpeg | dream-02.webp | 1200×800 |
| 03 | 迟到 | 迟到01.jpeg | dream-03.webp | 1200×800 |
| 04 | 被蛇咬 | 被蛇咬.jpeg | dream-04.webp | 1200×800 |
| 05 | 与前任复合 | 复合.jpeg | dream-05.webp | 1200×800 |
| 06 | 飞翔 | 飞翔.jpeg | dream-06.webp | 1200×800 |
| 07 | 溺水 | 溺水.jpeg | dream-07.webp | 1200×800 |
| 08 | 被追赶 | 被追赶.jpeg | dream-08.webp | 1200×800 |
| 09 | 迷路 | 迷宫.jpeg | dream-09.webp | 1200×800 |
| 10 | 死亡 | 死亡.jpeg | dream-10.webp | 1200×800 |

#### 批量转换脚本

```bash
#!/bin/bash

# 配置
QUALITY=90
WIDTH=1200
HEIGHT=800

echo "🎨 开始转换梦境配图..."

mkdir -p public/assets/dreams

# 转换
for file in design-exports/dreams/*.{jpeg,jpg,png}; do
  [ -f "$file" ] || continue

  # 提取文件名中的数字（如果有）
  filename=$(basename "$file")
  name="${filename%.*}"

  # 生成输出文件名
  if [[ $name =~ ^([0-9]+) ]]; then
    num=$(printf "%02d" ${BASH_REMATCH[1]})
    output="dream-${num}.webp"
  else
    # 根据文件名映射
    case $name in
      "坠落") output="dream-01.webp";;
      "生病") output="dream-02.webp";;
      "迟到"*) output="dream-03.webp";;
      "被蛇咬") output="dream-04.webp";;
      "复合") output="dream-05.webp";;
      "飞翔") output="dream-06.webp";;
      "溺水") output="dream-07.webp";;
      "被追赶") output="dream-08.webp";;
      "迷宫") output="dream-09.webp";;
      "死亡") output="dream-10.webp";;
      *) output="${name}.webp";;
    esac
  fi

  cwebp -resize $WIDTH $HEIGHT -q $QUALITY \
    "$file" \
    -o "public/assets/dreams/$output"

  echo "✅ $filename → $output"
done

echo "🎉 梦境配图转换完成！"
```

### 3.3 数据准备

#### 梦境数据结构

已创建文件：`public/data/dreamData.json`

**验证数据：**

```bash
# 检查 JSON 格式是否正确
node -e "console.log(JSON.parse(require('fs').readFileSync('public/data/dreamData.json')))"

# 输出：应该显示完整的数据对象
```

### 3.4 资源准备检查清单

- [ ] **背景图准备完成**
  - [ ] home-bg-2k.webp (2560×1440, 250-350KB)
  - [ ] nav-bg-2k.webp (2560×1440, 250-350KB)
  - [ ] detail-bg-2k.webp (2560×1440, 250-350KB)

- [ ] **梦境配图准备完成**
  - [ ] dream-01.webp ~ dream-10.webp (1200×800, 各100-200KB)

- [ ] **数据文件准备完成**
  - [ ] dreamData.json 格式正确
  - [ ] 10个梦境数据完整

- [ ] **资源总大小检查**
  - [ ] 背景图总大小 < 1MB
  - [ ] 配图总大小 < 2MB
  - [ ] 总资源 < 3MB

---

## 4. 开发阶段规划

### Phase 1: 项目初始化（2天）

#### 任务清单

**Day 1: 环境搭建**

- [ ] **创建 Vite + React + TypeScript 项目**
  ```bash
  npm create vite@latest dreambook -- --template react-ts
  cd dreambook
  npm install
  ```

- [ ] **安装核心依赖**
  ```bash
  npm install react-router-dom gsap
  npm install -D @types/node
  ```

- [ ] **配置 TypeScript**
  - 配置 tsconfig.json
  - 创建类型定义文件

- [ ] **配置 Vite**
  ```typescript
  // vite.config.ts
  import { defineConfig } from 'vite'
  import react from '@vitejs/plugin-react'
  import path from 'path'

  export default defineConfig({
    plugins: [react()],
    resolve: {
      alias: {
        '@': path.resolve(__dirname, './src'),
        '@components': path.resolve(__dirname, './src/components'),
        '@pages': path.resolve(__dirname, './src/pages'),
        '@utils': path.resolve(__dirname, './src/utils'),
      }
    },
    build: {
      outDir: 'dist',
      assetsDir: 'assets',
      rollupOptions: {
        output: {
          manualChunks: {
            'react-vendor': ['react', 'react-dom', 'react-router-dom'],
            'animation': ['gsap']
          }
        }
      }
    }
  })
  ```

**Day 2: 基础架构**

- [ ] **创建项目目录结构**
  ```bash
  mkdir -p src/{components,pages,hooks,utils,types,styles,animations}
  mkdir -p src/components/{Background,Card,Navigation,Settings}
  mkdir -p src/pages/{HomePage,NavigationPage,DetailPage}
  mkdir -p public/assets/{backgrounds,dreams}
  mkdir -p public/data
  ```

- [ ] **配置路由**
  ```typescript
  // src/App.tsx
  import { BrowserRouter, Routes, Route } from 'react-router-dom'
  import HomePage from '@pages/HomePage/HomePage'
  import NavigationPage from '@pages/NavigationPage/NavigationPage'
  import DetailPage from '@pages/DetailPage/DetailPage'

  function App() {
    return (
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<HomePage />} />
          <Route path="/navigation" element={<NavigationPage />} />
          <Route path="/dream/:id" element={<DetailPage />} />
        </Routes>
      </BrowserRouter>
    )
  }
  ```

- [ ] **创建全局样式**
  - 创建 CSS 变量
  - 重置样式
  - 字体配置

- [ ] **创建类型定义**
  ```typescript
  // src/types/dream.d.ts
  export interface Dream {
    id: string
    title: string
    subtitle: string
    emotion: string
    emotionTag: EmotionTag
    image: string
    description: string
    intro: string
    reasons: Reason[]
    solutions: Solution[]
    summary: string
    backgroundConfig: BackgroundConfig
  }

  export type EmotionTag = 'JOY' | 'SAD' | 'FEAR' | 'CALM' | 'STRESS'

  export interface BackgroundConfig {
    gradient: string
    particleColor: string
    particleSpeed: number
    particleDirection: string
    fogSpeed: number
  }
  ```

---

### Phase 2: 基础视觉实现（3-4天）

#### Day 3-4: CSS 基础背景系统

- [ ] **实现渐变背景组件**
  ```typescript
  // src/components/Background/GradientBackground.tsx
  import { useEffect, useState } from 'react'
  import styles from './GradientBackground.module.css'

  interface Props {
    gradient: string
    transitionDuration?: number
  }

  export default function GradientBackground({
    gradient,
    transitionDuration = 1000
  }: Props) {
    const [currentGradient, setCurrentGradient] = useState(gradient)

    useEffect(() => {
      setCurrentGradient(gradient)
    }, [gradient])

    return (
      <div
        className={styles.background}
        style={{
          background: currentGradient,
          transition: `background ${transitionDuration}ms ease-in-out`
        }}
      />
    )
  }
  ```

- [ ] **实现 Canvas 2D 粒子系统**
  ```typescript
  // src/components/Background/ParticleCanvas.tsx
  import { useEffect, useRef } from 'react'

  interface Particle {
    x: number
    y: number
    vx: number
    vy: number
    size: number
    opacity: number
  }

  interface Props {
    particleCount?: number
    particleColor?: string
    particleSpeed?: number
  }

  export default function ParticleCanvas({
    particleCount = 300,
    particleColor = '#ffffff',
    particleSpeed = 1
  }: Props) {
    const canvasRef = useRef<HTMLCanvasElement>(null)
    const particlesRef = useRef<Particle[]>([])

    // 初始化粒子
    // 渲染循环
    // 动画逻辑

    return (
      <canvas
        ref={canvasRef}
        style={{
          position: 'fixed',
          top: 0,
          left: 0,
          width: '100%',
          height: '100%',
          pointerEvents: 'none'
        }}
      />
    )
  }
  ```

- [ ] **实现毛玻璃卡片组件**
  ```css
  /* GlassCard.module.css */
  .glassCard {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    border-radius: 24px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
  }
  ```

#### Day 5-6: 页面布局

- [ ] **首页布局**
  - Logo 和标题区域
  - 主CTA按钮
  - 背景层叠加

- [ ] **导航页布局**
  - 搜索框
  - 10个梦境卡片网格
  - 返回主页按钮

- [ ] **详情页布局**
  - 标题区域
  - 配图展示
  - 内容区域（原因、建议）
  - 导航按钮

---

### Phase 3: 动画系统实现（2-3天）

#### Day 7-8: GSAP 动画

- [ ] **页面进场动画**
  ```typescript
  // src/animations/pageTransitions.ts
  import gsap from 'gsap'

  export const homePageEnter = (elements: {
    logo: HTMLElement
    title: HTMLElement
    subtitle: HTMLElement
    button: HTMLElement
  }) => {
    const tl = gsap.timeline()

    tl.from(elements.logo, {
      y: -30,
      opacity: 0,
      duration: 0.8,
      ease: 'power3.out'
    })
    .from(elements.title, {
      y: 20,
      opacity: 0,
      duration: 0.6,
      ease: 'power2.out'
    }, '-=0.3')
    .from(elements.subtitle, {
      y: 20,
      opacity: 0,
      duration: 0.6,
      ease: 'power2.out'
    }, '-=0.4')
    .from(elements.button, {
      scale: 0.9,
      opacity: 0,
      duration: 0.5,
      ease: 'back.out(1.4)'
    }, '-=0.3')

    return tl
  }
  ```

- [ ] **卡片动画**
  ```typescript
  // src/animations/cardAnimations.ts
  export const cardGridEnter = (cards: HTMLElement[]) => {
    return gsap.from(cards, {
      y: 30,
      opacity: 0,
      duration: 0.6,
      stagger: 0.08,
      ease: 'power2.out'
    })
  }

  export const cardHover = (card: HTMLElement) => {
    gsap.to(card, {
      y: -4,
      scale: 1.02,
      duration: 0.3,
      ease: 'power2.out'
    })
  }
  ```

- [ ] **情绪背景切换动画**
  ```typescript
  // src/animations/emotionMappings.ts
  export const switchEmotionBackground = (
    fromEmotion: EmotionTag,
    toEmotion: EmotionTag,
    duration = 1000
  ) => {
    // 平滑过渡背景配置
    // 同时改变粒子颜色、速度、方向
  }
  ```

#### Day 9: 交互动画优化

- [ ] **按钮交互反馈**
- [ ] **页面转场效果**
- [ ] **滚动动画**

---

### Phase 4: 数据集成（1-2天）

#### Day 10-11: 数据层

- [ ] **加载梦境数据**
  ```typescript
  // src/utils/dreamDataLoader.ts
  import dreamData from '@/public/data/dreamData.json'

  export const getDreams = () => dreamData.dreams
  export const getDreamById = (id: string) =>
    dreamData.dreams.find(d => d.id === id)
  export const getEmotionConfig = (tag: EmotionTag) =>
    dreamData.emotionTypes[tag]
  ```

- [ ] **实现搜索功能**
  ```typescript
  export const searchDreams = (query: string) => {
    return dreamData.dreams.filter(dream =>
      dream.title.includes(query) ||
      dream.description.includes(query) ||
      dream.emotion.includes(query)
    )
  }
  ```

- [ ] **情绪筛选**
- [ ] **内容渲染逻辑**

---

### Phase 5: 性能优化（2-3天）

#### Day 12-13: 性能监控系统

- [ ] **实现性能监控 Hook**
  ```typescript
  // src/hooks/usePerformanceMonitor.ts
  import { useEffect, useState } from 'react'

  interface PerformanceMetrics {
    fps: number
    memory: number
    gpuUsage: number
  }

  export const usePerformanceMonitor = () => {
    const [metrics, setMetrics] = useState<PerformanceMetrics>({
      fps: 60,
      memory: 0,
      gpuUsage: 0
    })

    useEffect(() => {
      // 监控 FPS
      // 监控内存
      // 自动降级逻辑
    }, [])

    return metrics
  }
  ```

- [ ] **实现模式切换**
  ```typescript
  // src/components/Settings/PerformanceToggle.tsx
  export default function PerformanceToggle() {
    const [mode, setMode] = useState<'HIGH' | 'STANDARD' | 'ECO'>('STANDARD')

    const handleModeChange = (newMode) => {
      setMode(newMode)
      // 切换背景系统
      // 调整粒子数量
    }

    return (
      <div>
        <button onClick={() => handleModeChange('HIGH')}>高品质</button>
        <button onClick={() => handleModeChange('STANDARD')}>标准</button>
        <button onClick={() => handleModeChange('ECO')}>省电</button>
      </div>
    )
  }
  ```

#### Day 14: 优化策略

- [ ] **图片懒加载**
  ```typescript
  const ImageLazy = ({ src, alt }: { src: string, alt: string }) => {
    const [loaded, setLoaded] = useState(false)

    return (
      <img
        src={loaded ? src : 'placeholder.webp'}
        alt={alt}
        loading="lazy"
        onLoad={() => setLoaded(true)}
      />
    )
  }
  ```

- [ ] **预加载关键资源**
  ```typescript
  useEffect(() => {
    // 预加载下一页的背景图
    const preloadImages = [
      '/assets/backgrounds/nav-bg-2k.webp',
      '/assets/backgrounds/detail-bg-2k.webp'
    ]

    preloadImages.forEach(src => {
      const img = new Image()
      img.src = src
    })
  }, [])
  ```

- [ ] **空闲检测与省电模式**
  ```typescript
  // src/hooks/useIdleDetection.ts
  export const useIdleDetection = (timeout = 180000) => {
    const [isIdle, setIsIdle] = useState(false)

    useEffect(() => {
      let timer: NodeJS.Timeout

      const resetTimer = () => {
        clearTimeout(timer)
        setIsIdle(false)
        timer = setTimeout(() => setIsIdle(true), timeout)
      }

      window.addEventListener('mousemove', resetTimer)
      window.addEventListener('touchstart', resetTimer)

      return () => {
        window.removeEventListener('mousemove', resetTimer)
        window.removeEventListener('touchstart', resetTimer)
      }
    }, [timeout])

    return isIdle
  }
  ```

---

### Phase 6: Three.js 增强（可选，2-3天）

#### Day 15-17: 高级背景系统

- [ ] **Three.js 场景搭建**
  ```typescript
  // src/components/Background/ThreeBackground.tsx
  import { useEffect, useRef } from 'react'
  import * as THREE from 'three'

  export default function ThreeBackground() {
    const containerRef = useRef<HTMLDivElement>(null)
    const sceneRef = useRef<THREE.Scene>()
    const rendererRef = useRef<THREE.WebGLRenderer>()

    useEffect(() => {
      // 初始化 Scene, Camera, Renderer
      // 创建粒子系统
      // 创建云雾 Shader
      // 渲染循环

      return () => {
        // 清理资源
        sceneRef.current?.clear()
        rendererRef.current?.dispose()
      }
    }, [])

    return <div ref={containerRef} />
  }
  ```

- [ ] **Shader 云雾效果**
- [ ] **高级粒子系统**
- [ ] **性能检测与自动降级**

**⚠️ 注意：** 这个阶段可以在拿到真实硬件后再决定是否实施

---

### Phase 7: 触摸屏优化（1-2天）

#### Day 18-19: 触摸交互

- [ ] **触摸事件适配**
  ```typescript
  const handleTouch = (e: React.TouchEvent) => {
    e.preventDefault()
    // 处理触摸逻辑
  }
  ```

- [ ] **点击区域优化**
  ```css
  /* 所有可点击元素 */
  .clickable {
    min-width: 48px;
    min-height: 48px;
    padding: 12px 24px;
  }
  ```

- [ ] **滑动手势支持**
- [ ] **长按返回主页**
  ```typescript
  let pressTimer: NodeJS.Timeout

  const handleLongPress = () => {
    pressTimer = setTimeout(() => {
      navigate('/')
    }, 1000)
  }

  const handleRelease = () => {
    clearTimeout(pressTimer)
  }
  ```

- [ ] **防误触优化**
- [ ] **禁用双指缩放**
  ```html
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  ```

---

### Phase 8: Electron 打包（1-2天）

#### Day 20-21: 桌面应用

- [ ] **安装 Electron**
  ```bash
  npm install -D electron electron-builder
  ```

- [ ] **配置 Electron 主进程**
  ```javascript
  // electron/main.js
  const { app, BrowserWindow } = require('electron')
  const path = require('path')

  function createWindow() {
    const win = new BrowserWindow({
      width: 1920,
      height: 1080,
      fullscreen: true,
      frame: false,
      kiosk: true,  // 全屏模式
      webPreferences: {
        preload: path.join(__dirname, 'preload.js'),
        contextIsolation: true
      }
    })

    // 加载应用
    if (process.env.NODE_ENV === 'development') {
      win.loadURL('http://localhost:5173')
    } else {
      win.loadFile(path.join(__dirname, '../dist/index.html'))
    }

    // 隐藏菜单栏
    win.setMenuBarVisibility(false)
  }

  app.whenReady().then(createWindow)
  ```

- [ ] **配置打包脚本**
  ```json
  // package.json
  {
    "scripts": {
      "dev": "vite",
      "build": "tsc && vite build",
      "electron:dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && electron .\"",
      "electron:build": "npm run build && electron-builder"
    },
    "build": {
      "appId": "com.dreambook.emotionplanet",
      "productName": "情绪星球",
      "directories": {
        "output": "release"
      },
      "files": [
        "dist/**/*",
        "electron/**/*"
      ],
      "win": {
        "target": "nsis",
        "icon": "build/icon.ico"
      }
    }
  }
  ```

- [ ] **测试 Electron 应用**
- [ ] **优化启动性能**

---

### Phase 9: 测试与优化（2-3天）

#### Day 22-24: 全面测试

详见 [第5章：测试验收标准](#5-测试验收标准)

---

## 5. 测试验收标准

### 5.1 性能测试

#### 性能指标

| 指标 | 目标值 | 测试方法 | 优先级 |
|-----|--------|---------|--------|
| **首屏加载** | < 2秒 | Performance API | P0 |
| **页面切换** | < 0.8秒 | 手动计时 | P0 |
| **帧率** | ≥ 55fps | Chrome DevTools | P0 |
| **内存占用** | < 400MB | Task Manager（2小时后） | P0 |
| **CPU占用（空闲）** | < 15% | Task Manager | P1 |
| **GPU占用** | < 30% | GPU-Z | P1 |

#### 测试脚本

```typescript
// src/utils/performanceTest.ts
export const measurePerformance = () => {
  // 首屏加载
  const loadTime = performance.timing.loadEventEnd - performance.timing.navigationStart
  console.log('首屏加载时间:', loadTime, 'ms')

  // FPS 监控
  let lastTime = performance.now()
  let frames = 0

  const measureFPS = () => {
    frames++
    const currentTime = performance.now()
    if (currentTime >= lastTime + 1000) {
      const fps = Math.round((frames * 1000) / (currentTime - lastTime))
      console.log('FPS:', fps)
      frames = 0
      lastTime = currentTime
    }
    requestAnimationFrame(measureFPS)
  }

  requestAnimationFrame(measureFPS)

  // 内存监控
  if ((performance as any).memory) {
    const memory = (performance as any).memory
    console.log('内存占用:', {
      used: (memory.usedJSHeapSize / 1048576).toFixed(2) + ' MB',
      total: (memory.totalJSHeapSize / 1048576).toFixed(2) + ' MB'
    })
  }
}
```

### 5.2 功能测试清单

#### 首页测试

- [ ] **视觉检查**
  - [ ] 背景渐变正确显示
  - [ ] 粒子动画流畅
  - [ ] Logo 和标题清晰可读
  - [ ] 按钮样式正确

- [ ] **动画测试**
  - [ ] 进场动画按顺序播放
  - [ ] Logo 从上方滑入
  - [ ] 标题淡入
  - [ ] 按钮缩放进场
  - [ ] 呼吸动画平滑

- [ ] **交互测试**
  - [ ] 点击主按钮跳转到导航页
  - [ ] 按钮有点击反馈动画
  - [ ] 触摸响应灵敏

#### 导航页测试

- [ ] **布局检查**
  - [ ] 10个卡片正确排列
  - [ ] 搜索框显示正常
  - [ ] 返回按钮可见

- [ ] **卡片测试**
  - [ ] 所有卡片图片加载成功
  - [ ] 卡片标题文字正确
  - [ ] 悬停效果正常（上浮+发光）
  - [ ] 点击卡片跳转到详情页

- [ ] **搜索功能**
  - [ ] 输入关键词能筛选梦境
  - [ ] 搜索结果实时更新
  - [ ] 无结果时显示提示

- [ ] **情绪切换**
  - [ ] 切换情绪标签时背景平滑过渡
  - [ ] 粒子颜色和速度改变
  - [ ] 过渡时间约1秒

#### 详情页测试

- [ ] **内容显示**
  - [ ] 标题正确
  - [ ] 配图显示
  - [ ] 梦境描述完整
  - [ ] 原因分析列表显示
  - [ ] 解决建议列表显示
  - [ ] 总结文字显示

- [ ] **滚动测试**
  - [ ] 内容可以流畅滚动
  - [ ] 滚动条样式美观
  - [ ] 长内容不会溢出

- [ ] **导航测试**
  - [ ] 返回按钮工作正常
  - [ ] 上一个/下一个梦境按钮工作
  - [ ] 长按返回主页功能

### 5.3 触摸屏专项测试

- [ ] **点击测试**
  - [ ] 所有按钮可点击区域 ≥ 48×48px
  - [ ] 单指点击响应正常
  - [ ] 点击无延迟（< 100ms）

- [ ] **手势测试**
  - [ ] 滑动流畅
  - [ ] 双指缩放已禁用
  - [ ] 长按识别准确

- [ ] **防误触**
  - [ ] 快速连续点击不会卡顿
  - [ ] 边缘区域点击响应正常

### 5.4 长时间运行测试

#### 测试方案

```
测试时长：8 小时连续运行
测试内容：
  - 每 30 分钟记录性能指标
  - 每 1 小时手动交互测试
  - 观察是否出现卡顿、崩溃

检查点：
  ✅ 1小时后：内存是否稳定
  ✅ 4小时后：是否出现掉帧
  ✅ 8小时后：是否能正常运行
```

#### 记录表格

| 时间 | 内存占用 | FPS | CPU% | GPU% | 备注 |
|-----|---------|-----|------|------|------|
| 0:00 | - | - | - | - | 启动 |
| 0:30 | - | - | - | - | - |
| 1:00 | - | - | - | - | - |
| ... | - | - | - | - | - |
| 8:00 | - | - | - | - | 结束 |

### 5.5 验收通过标准

#### 必须通过（P0）

- ✅ 所有页面正常显示
- ✅ 10个梦境数据完整
- ✅ 页面切换流畅（< 0.8秒）
- ✅ 帧率稳定（≥ 55fps）
- ✅ 8小时连续运行无崩溃
- ✅ 内存占用 < 400MB

#### 建议通过（P1）

- ⭐ 动画细节完美
- ⭐ 触摸响应 < 50ms
- ⭐ 所有过渡动画柔和
- ⭐ 背景情绪切换平滑

---

## 6. 部署上线流程

### 6.1 生产环境构建

#### Step 1: 最终优化

```bash
# 清理项目
npm run clean

# 重新安装依赖
rm -rf node_modules package-lock.json
npm install

# 运行最终检查
npm run lint
npm run type-check
```

#### Step 2: 构建生产版本

```bash
# 构建 Web 版本
npm run build

# 检查构建产物
ls -lh dist/

# 预期大小：
# - index.html: < 5KB
# - assets/index-xxx.js: < 500KB
# - assets/index-xxx.css: < 50KB
# - assets/backgrounds/: < 1MB
# - assets/dreams/: < 2MB
```

#### Step 3: Electron 打包

```bash
# 打包 Windows 版本
npm run electron:build

# 输出位置：release/
# 文件：情绪星球 Setup 1.0.0.exe
```

### 6.2 安装与部署

#### 图书馆设备部署清单

- [ ] **准备安装文件**
  - [ ] 情绪星球 Setup.exe
  - [ ] 配置文件（如有）
  - [ ] 用户手册

- [ ] **设备环境检查**
  - [ ] Windows 10 版本 ≥ 1903
  - [ ] 显示分辨率：1920×1080
  - [ ] 触摸屏驱动正常
  - [ ] 磁盘空间 ≥ 1GB

- [ ] **安装步骤**
  ```
  1. 双击 Setup.exe
  2. 选择安装路径
  3. 完成安装
  4. 创建桌面快捷方式
  5. 设置开机自启动
  ```

- [ ] **配置开机自启**
  ```
  方法1：开机文件夹
  Win + R → shell:startup
  复制快捷方式到此文件夹

  方法2：注册表（更可靠）
  Win + R → regedit
  HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run
  新建字符串值：Dreambook
  值：C:\Program Files\情绪星球\情绪星球.exe
  ```

### 6.3 首次运行检查

- [ ] **启动测试**
  - [ ] 应用正常启动
  - [ ] 全屏模式正确
  - [ ] 无菜单栏、任务栏

- [ ] **性能测试**
  - [ ] 首屏加载 < 3秒
  - [ ] 运行 2 小时无问题
  - [ ] 触摸响应灵敏

- [ ] **用户体验测试**
  - [ ] 邀请 5 位用户试用
  - [ ] 收集反馈
  - [ ] 记录问题

### 6.4 维护与监控

#### 日常维护

```
每日：
  - 开馆前检查设备是否正常启动
  - 闭馆后检查运行日志（如果有）

每周：
  - 检查设备性能
  - 清理缓存（如有必要）

每月：
  - 检查磁盘空间
  - 检查 Windows 更新
  - 备份配置文件
```

#### 问题处理

| 问题 | 可能原因 | 解决方案 |
|-----|---------|---------|
| 启动失败 | 文件损坏 | 重新安装 |
| 触摸失灵 | 驱动问题 | 检查触摸屏驱动 |
| 卡顿 | 内存不足 | 重启设备 |
| 黑屏 | GPU 问题 | 降低性能模式 |

---

## 7. 时间规划表

### 7.1 开发时间表（3-4周）

| 周次 | 阶段 | 主要任务 | 预计工时 | 验收标准 |
|-----|------|---------|---------|---------|
| **Week 1** | 准备+基础 | 环境搭建、资源准备、基础架构 | 40h | ✅ 项目可运行、路由正常 |
| **Week 2** | 核心开发 | 视觉实现、动画系统、数据集成 | 40h | ✅ 所有页面可访问、动画流畅 |
| **Week 3** | 优化+打包 | 性能优化、Electron打包、测试 | 40h | ✅ 性能达标、可安装 |
| **Week 4** | 测试+部署 | 长时间测试、真机部署、问题修复 | 40h | ✅ 通过验收、稳定运行 |

### 7.2 详细甘特图

```
Week 1: 2025-12-02 ~ 12-08
├─ Day 1-2: 项目初始化
│  ├─ 环境搭建 ████████░░ 80%
│  └─ 基础架构 ██████░░░░ 60%
├─ Day 3-4: 资源准备
│  ├─ 图片转换 ██████████ 100%
│  └─ 数据整理 ██████████ 100%
└─ Day 5-7: 基础视觉
   ├─ CSS 背景 ██████████ 100%
   ├─ Canvas 粒子 ████████░░ 80%
   └─ 页面布局 ██████░░░░ 60%

Week 2: 2025-12-09 ~ 12-15
├─ Day 8-9: GSAP 动画
│  ├─ 进场动画 ██████████ 100%
│  ├─ 卡片动画 ██████████ 100%
│  └─ 转场动画 ████████░░ 80%
├─ Day 10-11: 数据集成
│  ├─ 数据加载 ██████████ 100%
│  ├─ 搜索功能 ██████████ 100%
│  └─ 内容渲染 ██████████ 100%
└─ Day 12-14: 性能优化
   ├─ 性能监控 ████████░░ 80%
   ├─ 模式切换 ██████░░░░ 60%
   └─ 懒加载 ██████████ 100%

Week 3: 2025-12-16 ~ 12-22
├─ Day 15-17: Three.js (可选)
│  ├─ 场景搭建 ░░░░░░░░░░ 0% (待定)
│  └─ Shader 效果 ░░░░░░░░░░ 0% (待定)
├─ Day 18-19: 触摸优化
│  ├─ 触摸事件 ██████████ 100%
│  ├─ 手势支持 ████████░░ 80%
│  └─ 防误触 ██████████ 100%
└─ Day 20-21: Electron
   ├─ 配置打包 ██████████ 100%
   ├─ 测试应用 ████████░░ 80%
   └─ 优化启动 ██████░░░░ 60%

Week 4: 2025-12-23 ~ 12-29
├─ Day 22-24: 全面测试
│  ├─ 功能测试 ██████████ 100%
│  ├─ 性能测试 ████████░░ 80%
│  └─ 长时间测试 ██████░░░░ 60%
├─ Day 25-26: 真机部署
│  ├─ 设备安装 ░░░░░░░░░░ 0%
│  ├─ 配置优化 ░░░░░░░░░░ 0%
│  └─ 用户测试 ░░░░░░░░░░ 0%
└─ Day 27-28: 问题修复
   └─ Bug 修复 ░░░░░░░░░░ 0%
```

### 7.3 里程碑

| 里程碑 | 日期 | 交付物 | 验收人 |
|-------|------|--------|-------|
| **M1: 项目启动** | 12-02 | 环境搭建完成、资源准备完成 | 技术负责人 |
| **M2: Alpha 版本** | 12-15 | 所有功能完成、可运行 | 项目经理 |
| **M3: Beta 版本** | 12-22 | Electron 打包完成、性能达标 | 测试负责人 |
| **M4: RC 版本** | 12-27 | 通过所有测试 | 产品经理 |
| **M5: 正式发布** | 12-29 | 真机部署成功 | 甲方 |

---

## 8. 风险控制

### 8.1 技术风险

| 风险 | 概率 | 影响 | 缓解措施 | 应对方案 |
|-----|------|------|---------|---------|
| **Three.js 性能不足** | 中 | 高 | 先实现 Canvas 2D 方案 | 放弃 Three.js，使用纯 CSS+Canvas |
| **触摸屏不兼容** | 低 | 高 | 提前测试触摸事件 | 准备备用交互方案 |
| **长时间运行卡顿** | 中 | 中 | 实现性能监控 | 自动降级+定时重启 |
| **内存泄漏** | 中 | 高 | 严格资源释放 | 每天定时重启应用 |
| **Electron 打包失败** | 低 | 中 | 提前测试打包流程 | 使用 Web 版本 + 浏览器全屏 |

### 8.2 进度风险

| 风险 | 缓解措施 |
|-----|---------|
| **开发进度延迟** | 设置缓冲时间（第4周）、砍掉 Three.js 功能 |
| **资源准备不及时** | 提前2周开始准备、准备备用图片 |
| **测试时间不足** | 边开发边测试、自动化测试脚本 |
| **真机环境问题** | 提前获取设备信息、准备虚拟机测试 |

### 8.3 质量风险

| 风险 | 预防措施 | 检测方法 |
|-----|---------|---------|
| **性能不达标** | 严格遵循性能规范 | 每日性能测试 |
| **动画不流畅** | 使用 GSAP 专业库 | 肉眼观察+FPS监控 |
| **用户体验差** | 多次内部测试 | 用户测试反馈 |
| **兼容性问题** | 在真实环境测试 | 真机测试 |

---

## 9. 附录：技术决策记录

### 9.1 为什么选择 React + Vite？

**原因：**
- React 生态成熟，组件化开发效率高
- Vite 构建速度快，开发体验好
- TypeScript 提供类型安全
- 团队熟悉度高

**对比方案：**
- Vue 3：生态较小，学习成本高
- Svelte：生态不成熟，风险大

### 9.2 为什么选择 Canvas 2D + 可选 Three.js？

**原因：**
- Canvas 2D 性能稳定，兼容性好
- Three.js 作为增强选项，降低风险
- 双模式架构适应不同硬件

**对比方案：**
- 纯 CSS：效果有限，无法实现复杂粒子
- 纯 Three.js：性能风险高，不稳定

### 9.3 为什么使用 WebP 格式？

**原因：**
- 文件大小比 JPG 小 50%
- 渐变质量优于 JPG
- Electron 完美支持
- 加载速度更快

**对比方案：**
- JPG：文件大，质量稍差
- PNG：文件太大（2-4MB）

### 9.4 为什么选择 2560×1440 分辨率？

**原因：**
- 比 1080p 高 33%，避免色带
- 允许放大 1.2 倍做动画
- GPU 内存占用合理（8-12MB）
- 适合长时间运行

**对比方案：**
- 1920×1080：可能出现色带
- 3840×2160：性能负担过大

---

## 📋 最终检查清单

### 开发前检查

- [ ] ✅ 已阅读完整开发计划
- [ ] ✅ 已准备所有设计稿资源
- [ ] ✅ 已安装所有必要工具
- [ ] ✅ 已创建项目目录结构
- [ ] ✅ 已确认硬件环境信息

### 开发中检查

- [ ] ✅ 每日提交代码到 Git
- [ ] ✅ 遵循动画规范开发
- [ ] ✅ 每周进行性能测试
- [ ] ✅ 及时记录技术问题

### 上线前检查

- [ ] ✅ 通过所有测试用例
- [ ] ✅ 性能指标达标
- [ ] ✅ Electron 打包成功
- [ ] ✅ 准备用户手册
- [ ] ✅ 制定维护计划

---

## 📞 支持与帮助

### 技术文档

- Figma 设计稿：`/design/梦境解析.fig`
- 动画规范：`/docs/动画规则1.md`
- 梦境数据：`/docs/10个梦境数据.md`
- 执行规范：`/docs/项目执行规范.md`

### 工具链接

- React 文档：https://react.dev
- GSAP 文档：https://greensock.com/docs
- Three.js 文档：https://threejs.org/docs
- Electron 文档：https://www.electronjs.org/docs

---

**文档版本记录：**

| 版本 | 日期 | 修改内容 | 作者 |
|-----|------|---------|------|
| v1.0 | 2025-11-30 | 初始版本 | Claude |
| v2.0 | 2025-11-30 | 综合完整版 | Claude |

---

**祝开发顺利！🚀**
