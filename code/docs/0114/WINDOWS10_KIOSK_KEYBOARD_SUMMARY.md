# Windows 10 图书馆 Kiosk 模式虚拟键盘问题 - 执行摘要

> **项目：** DreamBook 梦境解析
> **环境：** Windows 10 图书馆部署
> **问题：** Admin 模式键盘正常，Kiosk 模式不弹出
> **文档日期：** 2026-01-14

---

## 🎯 核心结论

### ✅ 好消息：Windows 10 比 Windows 11 容易解决！

| 指标 | Windows 10（你们的环境）| Windows 11 |
|------|----------------------|-----------|
| **解决难度** | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐⭐ 极难 |
| **修复成功率** | **85-90%** ✅ | 50-60% |
| **所需时间** | **1 周** | 2-3 周 |
| **需要前端键盘** | ⚠️ 可选（备用）| ✅ 强烈推荐 |

**结论：优先使用修复现有代码方案，无需复杂的前端键盘。**

---

## 📊 问题分析

### 根本原因

```
Admin 账户（管理员权限）
  → TabTip.exe 有执行权限
  → 虚拟键盘正常 ✅

Kiosk 账户（受限用户）
  → TabTip.exe 无执行权限
  → 虚拟键盘失败 ❌
```

### Windows 10 特有问题

1. **权限不足**（主要问题）
   - Kiosk 账户无法执行 TabTip.exe
   - PowerShell COM 对象创建被拒绝

2. **Explorer.exe 依赖**（次要问题）
   - Windows 10 1809+ 版本引入
   - TabTip 进程启动，但 UI 不显示
   - 比 Windows 11 轻微

3. **代码问题**
   - `mainWindow.focus()` 导致焦点竞争
   - 键盘弹出后立即关闭

---

## 🛠️ 解决方案

### 方案 A：修复现有代码（强烈推荐 ⭐⭐⭐⭐⭐）

**适用性：** Windows 10 专用
**成功率：** 85-90%
**开发时间：** 1 周
**维护成本：** 低

#### 需要修复的 3 个关键点

**1. 修复注册表脚本**

当前脚本缺少 Windows 10 必需的配置。

**修改文件：** `code/dreambook/scripts/fix-keyboard-kiosk.ps1`

**在第 133 行后添加：**
```powershell
# ========== Windows 10 专用配置 ==========

# 键值 2：平板模式（Windows 10 推荐）
$regPath2 = "HKLM:\SOFTWARE\Microsoft\windows\CurrentVersion\ImmersiveShell"
if (-not (Test-Path $regPath2)) {
    New-Item -Path $regPath2 -Force | Out-Null
}
Set-ItemProperty -Path $regPath2 -Name "TabletMode" -Value 1 -Type DWord
Write-Host "  ✅ TabletMode = 1 (Windows 10)" -ForegroundColor Green

# 检测 Windows 版本并给出提示
$osVersion = (Get-WmiObject -Class Win32_OperatingSystem).Caption
Write-Host "  检测到系统: $osVersion" -ForegroundColor Cyan

if ($osVersion -like "*Windows 11*") {
    Write-Host "  ⚠️  警告：检测到 Windows 11，此脚本为 Windows 10 优化" -ForegroundColor Yellow
    Write-Host "     Windows 11 可能需要额外配置" -ForegroundColor Yellow
}
```

---

**2. 删除焦点调用**

`mainWindow.focus()` 在所有 Windows 版本都会导致键盘立即关闭。

**修改文件：** `code/dreambook/electron/main.ts`

**修改第 656-667 行：**
```typescript
// IPC: 显示虚拟键盘
ipcMain.handle('show-virtual-keyboard', () => {
  if (process.platform === 'win32') {
    // ❌ 删除这段代码：
    // if (mainWindow) {
    //   mainWindow.focus()
    //   log('[虚拟键盘] 窗口已获得焦点，准备显示键盘')
    // }

    // ✅ 直接显示键盘
    log('[虚拟键盘] 准备显示键盘')
    showWindowsVirtualKeyboard()
    return true
  }
  return false
})
```

---

**3. 添加进程检测**

避免重复启动 TabTip.exe。

**修改文件：** `code/dreambook/electron/main.ts`

**在第 295 行前添加：**
```typescript
import { execSync } from 'child_process'

/**
 * 检查 TabTip.exe 进程是否正在运行
 */
function isTabTipRunning(): boolean {
  try {
    const output = execSync('tasklist /FI "IMAGENAME eq TabTip.exe" /FO CSV /NH', {
      encoding: 'utf-8',
      stdio: ['pipe', 'pipe', 'ignore']
    })
    return output.includes('TabTip.exe')
  } catch {
    return false
  }
}

/**
 * 检测 Windows 版本
 */
function getWindowsVersion(): string {
  try {
    const output = execSync('ver', { encoding: 'utf-8' })
    if (output.includes('Windows 10')) return 'win10'
    if (output.includes('Windows 11')) return 'win11'
    return 'unknown'
  } catch {
    return 'unknown'
  }
}
```

**修改 `showWindowsVirtualKeyboard()` 函数（约第 329 行）：**
```typescript
function showWindowsVirtualKeyboard(): void {
  if (process.platform !== 'win32') {
    console.log('[虚拟键盘] 非 Windows 平台，跳过')
    return
  }

  // ✅ 检测 Windows 版本
  const winVersion = getWindowsVersion()
  log(`[虚拟键盘] Windows 版本: ${winVersion}`)

  // ✅ 检查进程是否已存在
  if (isTabTipRunning()) {
    log('[虚拟键盘] TabTip 已经在运行，跳过重复启动')
    return
  }

  // ... 其余代码保持不变
}
```

---

### 方案 B：前端虚拟键盘（备用方案）

**触发条件：** 方案 A 成功率 < 80%

**适用场景：**
- IT 部门禁止修改注册表
- 图书馆 Windows 10 版本差异大
- 需要 100% 稳定性

**实施：** 参考 `KIOSK_KEYBOARD_WIN10_ANALYSIS.md` 中的详细方案

---

## 📅 实施计划

### 第 1 周：快速修复（方案 A）

| 时间 | 任务 | 预计工时 | 负责人 |
|------|------|---------|--------|
| **Monday** | 修改注册表脚本 | 1h | 开发 |
| | 删除 focus 调用 | 0.5h | 开发 |
| | 添加进程检测 | 1h | 开发 |
| **Tuesday** | 本地测试（Admin） | 1h | 开发 |
| | 创建测试 Kiosk 账户 | 0.5h | 开发 |
| | Kiosk 账户测试 | 1h | 开发 |
| **Wednesday** | 打包新版本 | 1h | 开发 |
| | 完整测试 | 2h | QA |
| | 修复发现的问题 | 1h | 开发 |
| **Thursday** | 更新部署文档 | 1h | 开发 |
| | 准备部署脚本 | 1h | 运维 |
| **Friday** | 部署到图书馆测试设备 | 2h | 运维 |
| | 观察和收集反馈 | 2h | QA |

**总计：** 约 16 工时（2 人日）

---

### 第 2 周：观察与优化

**如果方案 A 成功率 ≥ 80%：**
- [ ] 批量部署到所有图书馆设备
- [ ] 收集用户反馈
- [ ] 完善文档

**如果方案 A 成功率 < 80%：**
- [ ] 启动方案 B（前端虚拟键盘）
- [ ] 预计 3-5 天开发
- [ ] 再测试

---

## 🔧 测试步骤

### 本地测试（部署前必做）

**1. 创建模拟 Kiosk 账户**
```powershell
# 以管理员身份运行 PowerShell
net user TestKiosk Password123! /add
net localgroup Users TestKiosk /add

# 确认不在管理员组
net user TestKiosk | findstr "Local Group Memberships"
```

**2. 以管理员身份运行修复脚本**
```powershell
cd C:\path\to\dreambook\scripts
.\fix-keyboard-kiosk.ps1

# 当提示输入 Kiosk 账户名时，输入：TestKiosk
```

**3. 切换到 TestKiosk 账户测试**
```
1. 注销当前账户
2. 登录 TestKiosk
3. 运行应用：C:\Program Files\梦境解析\梦境解析.exe
4. 点击搜索框
5. 观察虚拟键盘是否弹出
```

**4. 检查日志**
```
打开文件：C:\Users\TestKiosk\dreambook-logs\app-<日期>.log
搜索：[虚拟键盘]

期望看到：
[虚拟键盘] Windows 版本: win10
[虚拟键盘] ✅ PowerShell 执行成功
```

---

### 图书馆部署测试

**阶段 1：单设备测试（Friday）**
- 选择一台典型设备
- 以管理员身份安装新版本
- 运行修复脚本
- 在 Kiosk 账户测试 30 分钟
- 记录任何问题

**阶段 2：小范围部署（下周一）**
- 如果单设备成功，部署到 3-5 台设备
- 观察 1-2 天
- 收集用户反馈

**阶段 3：全面部署（下周三）**
- 如果小范围成功，批量部署
- 准备回滚方案（保留旧版本安装包）

---

## 🔍 故障排查

### 常见问题 1：修复脚本运行后仍不弹出

**诊断：**
```powershell
# 检查注册表
reg query "HKLM\SOFTWARE\Microsoft\TabletTip\1.7" /v EnableDesktopModeAutoInvoke

# 检查服务
sc query TabletInputService

# 检查文件权限
icacls "C:\Program Files\Common Files\microsoft shared\ink\TabTip.exe"

# 手动启动测试
"C:\Program Files\Common Files\microsoft shared\ink\TabTip.exe"
```

**如果手动启动成功 = 问题在应用代码**
**如果手动启动失败 = 权限仍有问题，重新运行脚本**

---

### 常见问题 2：TabTip 进程启动但看不到键盘

**原因：** Explorer.exe 依赖问题

**诊断：**
```powershell
# 检查 Explorer 是否运行
tasklist | findstr explorer.exe
```

**解决方案：**
- **方案 1：** 确保 Explorer.exe 运行（在 Kiosk 登录脚本中启动）
- **方案 2：** 使用前端虚拟键盘（推荐）

---

### 常见问题 3：PowerShell 执行策略被阻止

**诊断：**
```powershell
Get-ExecutionPolicy -List
```

**解决：**
```powershell
# 管理员运行
Set-ExecutionPolicy -Scope LocalMachine -ExecutionPolicy RemoteSigned -Force
```

**如果组策略禁止：**
- 联系 IT 部门申请例外
- 或使用前端虚拟键盘（不依赖 PowerShell）

---

## 📈 成功指标

### 定义成功

| 指标 | 目标 | 测量方法 |
|------|------|---------|
| **键盘弹出率** | ≥ 85% | 测试 100 次点击，成功次数 |
| **响应时间** | < 1 秒 | 从点击到键盘显示的时间 |
| **用户投诉** | < 5% | 用户反馈统计 |
| **稳定性** | 连续运行 7 天无故障 | 日志监控 |

### 监控方法

**1. 日志分析**
```powershell
# 每天检查日志
findstr /i "虚拟键盘.*失败\|error\|denied" C:\Users\kiosk\dreambook-logs\app-*.log
```

**2. 用户反馈收集**
- 在应用中添加"键盘问题反馈"按钮
- 记录失败次数到日志

**3. 定期测试**
- 每周在 Kiosk 账户测试 10 次
- 记录成功率

---

## 📚 相关文档

| 文档 | 路径 | 说明 |
|------|------|------|
| **本摘要** | `docs/0113/WINDOWS10_KIOSK_KEYBOARD_SUMMARY.md` | 执行摘要和快速参考 |
| **Windows 10 深度分析** | `docs/0113/KIOSK_KEYBOARD_WIN10_ANALYSIS.md` | 完整技术分析（20 页） |
| **通用分析（Win10/11）** | `docs/0113/KIOSK_KEYBOARD_ANALYSIS.md` | 跨版本对比分析 |
| **修复脚本** | `dreambook/scripts/fix-keyboard-kiosk.ps1` | 自动化配置脚本 |
| **现有解决方案文档** | `docs/0113/KIOSK_KEYBOARD_FIX.md` | 之前的解决方案记录 |

---

## 🎯 关键要点

### Windows 10 vs Windows 11

```
Windows 10 (你们的环境):
  ✅ 修复脚本成功率 85-90%
  ✅ 只需修改代码 + 运行脚本
  ✅ 1 周完成
  ⚠️ 部分设备可能需要前端键盘

Windows 11:
  ⚠️ 修复脚本成功率 50-60%
  ⚠️ 需要额外配置 EdgeTargetMode
  ⚠️ 强烈推荐使用前端键盘
  ⚠️ 2-3 周完成
```

### 修复的 3 个关键点

1. **注册表脚本** - 添加 `TabletMode = 1`
2. **删除 focus 调用** - 避免焦点竞争
3. **进程检测** - 避免重复启动

### 实施优先级

```
第 1 优先级：方案 A（修复现有代码）
  ↓ 预计成功率 85-90%
  ↓
第 2 优先级：方案 B（前端键盘）
  ↓ 作为备用方案
  ↓
预期结果：100% 设备可用
```

---

## ✅ 行动清单

### 立即执行（本周）

- [ ] **Monday AM**：修改注册表脚本（添加 TabletMode）
- [ ] **Monday PM**：删除 mainWindow.focus() 调用
- [ ] **Tuesday AM**：添加进程检测和版本检测
- [ ] **Tuesday PM**：本地测试（Admin + 模拟 Kiosk）
- [ ] **Wednesday**：打包新版本，完整测试
- [ ] **Thursday**：更新部署文档，准备脚本
- [ ] **Friday**：部署到图书馆测试设备

### 下周跟进

- [ ] **周一**：收集测试设备反馈
- [ ] **周二**：根据反馈调整（如需要）
- [ ] **周三**：决定是否批量部署
- [ ] **周四-周五**：批量部署（如果测试成功）

### 备用计划

- [ ] 如果方案 A 成功率 < 80%，启动方案 B（前端键盘）
- [ ] 预留 3-5 天开发时间

---

## 📞 支持

### 技术问题

**检查日志：**
```
C:\Users\<kiosk账户>\dreambook-logs\app-<日期>.log
```

**搜索错误：**
```powershell
findstr /i "虚拟键盘" app-*.log
```

### 参考文档

- **详细分析：** 查看 `KIOSK_KEYBOARD_WIN10_ANALYSIS.md`
- **通用方案：** 查看 `KIOSK_KEYBOARD_ANALYSIS.md`

---

## 📝 更新记录

| 日期 | 版本 | 说明 | 作者 |
|------|------|------|------|
| 2026-01-14 | 1.0 | 初始版本（Windows 10 专用分析）| Claude |

---

*这是一个执行摘要文档，用于快速参考和实施指导。*
*详细技术分析请参考 `KIOSK_KEYBOARD_WIN10_ANALYSIS.md`。*
